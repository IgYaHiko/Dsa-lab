#include<iostream>
#include<vector>
using namespace std;

class Solution {
    int bsearch(vector<int> &nums) {
        int i = 0;
        int j = nums.size()-1;

        while( i <= j) {
            int mid = i + (j - i) /2;
        }
    }
};

int main() {
    vector<int> nums = {1,2,3,4,6,8};
}